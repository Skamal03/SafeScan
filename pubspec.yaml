name: safescan
description: SafeScan - Mobile Security Utility App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^1.1.0
  network_info_plus: ^8.1.0
  permission_handler: ^12.0.1
  wifi_scan: ^0.4.0+1
  device_info_plus: ^13.1.0
  safe_device: ^1.1.4
  flutter_device_apps: ^0.7.0
  crypto: ^3.0.3
  firebase_core: ^4.9.0
  firebase_auth: ^6.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0


flutter:
  uses-material-design: true
